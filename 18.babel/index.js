/*
    # Install Babel
        + Open Terminal : Ctrl + `
        + Initializer : npm init
        + Install Nodemon : npm install --save-dev @babel/core @babel/cli @babel/preset-env
        + Add a build script to package.json : "build": "babel index.js -w -o build/index.js"
        + Run Build : npm run build
        + You will see a new build folder.
*/

class Star {}
const hello = () => {};
const yes = Promise.resolve(1);
